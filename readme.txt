Qt Openstreepmap viewer
---------------------------

This Project contains a cross-platform qt-based openstreetmap(OSM) viewer, which contains most popular features such like:
	* A plugin system provides layer extension.
	* A win32 active-X warpper , provides cross env usages.
	* 2 sample plugin examples, demonstrating how plugin works.

